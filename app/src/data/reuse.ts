/* 장롱 방지 — 유형별 재사용 판정 축.

   이 마켓의 기준은 "그래픽이 좋냐"가 아니라 "산 에셋이 장롱이 되느냐, 계속
   쓰이느냐"다. 그리고 사느냐 마느냐를 가르는 축은 유형마다 다르다 —
   캐릭터는 그림보다 애니메이션이 먼저고, 배경·소품은 아트 결만 맞으면 어느
   프로젝트에나 재사용된다. 단일 품질 점수 하나로는 이걸 못 담는다.

   에셋을 심판하지 않는다. "이 유형에서 구매자가 먼저 보는 것"을 알려 줄 뿐이라,
   특정 상품을 깎지 않으면서 장롱을 피하게 한다. 올리는 쪽엔 무엇을 채워야
   팔리는지, 사는 쪽엔 무엇을 보고 골라야 하는지 같은 표다. */

export type ReuseAxis = {
  /** 이 유형에서 사느냐 마느냐를 가르는 축 */
  axis: string;
  /** 왜 이게 핵심인지 — 장롱과 연결 */
  why: string;
  /** 재사용 난도(장롱 위험). 낮을수록 어느 프로젝트에나 다시 쓰기 쉽다. */
  risk: "낮음" | "보통" | "높음";
};

/* 키는 마켓 분류(CatKey)와 업로드 분류가 공유하는 문자열이다. */
export const REUSE_AXIS: Record<string, ReuseAxis> = {
  char: {
    axis: "필요한 애니메이션 포함",
    why: "캐릭터는 그림보다 모션이 먼저다. 원하는 동작이 없으면 아무리 잘 만들어도 못 쓴다.",
    risk: "높음",
  },
  weapon: {
    axis: "그립·리깅 호환",
    why: "캐릭터 손 본과 안 맞으면 장착이 안 된다. 그림체와 리깅 규격을 먼저 본다.",
    risk: "높음",
  },
  env: {
    axis: "아트 결 호환 (PBR·로폴·복셀)",
    why: "결이 프로젝트와 섞이면 배경만 튄다. 폴리·재질 방식이 맞아야 재사용된다.",
    risk: "보통",
  },
  light: {
    axis: "렌더 파이프라인 호환",
    why: "URP·HDRP·빌트인이 맞아야 켜진다. 파이프라인이 어긋나면 아예 안 보인다.",
    risk: "보통",
  },
  furniture: {
    axis: "아트 결·스케일",
    why: "결과 크기만 맞으면 실내 어느 씬에나 다시 넣는다. 캐릭터보다 관대하다.",
    risk: "낮음",
  },
  prop: {
    axis: "아트 결·스케일",
    why: "결과 크기만 맞으면 어느 씬에나 재사용된다. 장롱 될 일이 가장 적다.",
    risk: "낮음",
  },
  tex: {
    axis: "타일링·PBR 채널 일관성",
    why: "채널·타일이 맞으면 모든 표면에 재사용된다. 사실상 안전군.",
    risk: "낮음",
  },
};
